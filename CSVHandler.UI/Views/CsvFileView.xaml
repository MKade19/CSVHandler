<UserControl x:Class="CSVHandler.UI.Views.CsvFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSVHandler.UI.Views"
             xmlns:viewModels="clr-namespace:CSVHandler.UI.ViewModels"
             xmlns:converters="clr-namespace:CSVHandler.UI.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:CsvFileViewModel}"
             d:DesignHeight="500" d:DesignWidth="1050">
    <UserControl.Resources>
        <converters:BoolToVisibleConverter x:Key="BoolToVisibleConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>
    <Grid>
        
    </Grid>
</UserControl>
